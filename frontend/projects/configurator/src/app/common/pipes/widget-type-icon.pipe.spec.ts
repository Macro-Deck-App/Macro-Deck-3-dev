import { WidgetTypeIconPipe } from './widget-type-icon.pipe';

describe('WidgetTypeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new WidgetTypeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
